Ticket ticket_shape_string_logic_1 progress
- Goal: make projection naming and file discovery resilient (file path scanning, alias normalization, strict proj_group).
- Plan: add unit/integration tests for _iter_weight_files, _infer_proj boundaries, and proj_group normalization/strict behavior; then implement fixes in collect_data/init_run.
- Changes made: added `tests/test_proj_group_normalization.py` covering file-path scanning, alias boundary matching, proj_group normalization, and strict unmatched behavior.
- Decisions / assumptions: use csv outputs in integration tests; disable mlx/quant schemes; stub mlx import in unit and subprocess runs.
- Commands run: none.
- Update: starting investigation into why two expected failures (proj_group normalization + strict unmatched) did not surface in test run.
- Findings: integration tests in `tests/test_proj_group_normalization.py` use regex patterns like `.*experts.*\\.(w1|w2|w3)\\.weight$` which do not match tensor names (extra escaping), so `_apply_rules` never runs and `_fallback_extract` handles proj inference.
- Outcome: fallback path already canonicalizes aliases and treats unknown proj as unmatched, so those tests pass despite missing proj_group normalization/strict behavior in `_apply_rules`.
- Commands run: `python - <<'PY' ...` to confirm regex with `\\.` does not match (`re.match` false).
- Update: fixed test regex patterns to use `\.` so `_apply_rules` is exercised; running targeted unittest.
- Commands run: `python -m unittest tests.test_proj_group_normalization` (failed: `ModuleNotFoundError: No module named 'numpy'`), `uv run python -m unittest tests.test_proj_group_normalization` (failed due to sandbox permissions accessing uv cache), reran with escalation.
- Result (escalated uv run): 4 failures as expected now that rules match:
  - `_iter_weight_files` file-path scan still yields nothing.
  - `_infer_proj` still matches substring (w1 hits w13).
  - proj_group canonicalization still yields raw aliases.
  - proj_group strict mode still emits matrix_stats rows.
- Update: implemented file-path handling in `_iter_weight_files`, boundary-safe `_infer_proj` with sentinel contains behavior, `proj_group_strict` handling in `_apply_rules`, and added `parsing.proj_group_strict` to init_run template.
- Commands run: `uv run python -m unittest tests.test_proj_group_normalization` (pass).
