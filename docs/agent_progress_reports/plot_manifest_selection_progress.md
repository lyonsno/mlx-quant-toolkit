# Plot Manifest Selection Progress

- 2026-03-06: Added fail-first contract tests for `build_plots.py` to cover `logs/plots_write_manifest.json`, config-driven `plots.artifact_keys` selection, and unknown plot-key rejection before implementation.
- 2026-03-07: Tightened the fail-first plotting contracts to use one canonical `B_quant_global_summary__w_rel_fro__median_by_proj_and_scheme.png` filename, require manifest entries to map to real on-disk PNGs, and treat unselected artifact keys as loader-boundary poison pills rather than mere suppressed writes.
- 2026-03-07: Strengthened tests again to require early unknown-key validation with zero side effects, enforce `plots.artifact_keys` during legacy-scan discovery as well as manifest-backed discovery, and require `plots_write_manifest.json` to audit at least one per-artifact error alongside successful writes.
- 2026-03-07: Added poison-pill coverage to unknown-key rejection, a multi-key `plots.artifact_keys` success path, and a manifest contract for `skipped` plot artifacts so the future audit log is pinned across `written`, `error`, and `skipped`.
- 2026-03-07: Isolated selection-only tests from manifest-audit tests, forced the manifest-backed selection case to rely on a custom manifest-only table path, moved unknown-key coverage to a direct pre-discovery validation contract, and added an all-skipped zero-write manifest case.
- 2026-03-07: Fixed an accidental custom-path dependency in the plot-manifest success test, added direct validation contracts for unsupported plot keys and malformed `plots.artifact_keys` shapes, added selection coverage for `B_quant_global_summary`, and relaxed the all-skipped case to assert no emitted PNGs rather than no precreated `plots/` directory.
- 2026-03-07: Tightened selection tests to verify `plots_write_manifest.json` matches the configured artifact set, added zero-side-effect assertions to the remaining invalid-config cases, and extended the partial-failure audit test so a later quant plot must still be written after a middle artifact error.
- 2026-03-07: Locked in config semantics for `plots: {}` and `plots.artifact_keys: null` as default selection, `plots.artifact_keys: []` as select-nothing, and duplicate keys as first-seen dedupe order, while narrowing malformed-shape validation to the remaining truly invalid cases.
- 2026-03-07: Added an end-to-end duplicate-key contract that reaches emitted PNGs and `plots_write_manifest.json`, plus malformed-key coverage for blank/whitespace strings with a less brittle `plots.artifact_keys` error assertion.
- 2026-03-07: Added fail-first best-effort contracts for selected-but-missing plot inputs, covering both "continue to later artifacts and record an error" and "all selected inputs missing still writes an error manifest" behavior.
- 2026-03-07: Closed additional coverage gaps with a unit-level `_plot_a_weight_global` aggregation contract, manifest-backed quant-only selection with poison-pill unselected artifacts, broader malformed `plots.artifact_keys` shape cases, and reduced brittle/duplicated stderr-side assertions.
- 2026-03-07: Tightened contracts again with duplicate-pair median assertions for quant plotting, stale/partial manifest fallback coverage for selected keys, minimal stable CLI error-prefix checks in load-error tests, and explicit empty-selection audit-manifest expectations.
- 2026-03-07: Resolved no-job manifest consistency across selection tests, added malformed `plots` container-shape validation before discovery, and broadened stale/partial manifest fallback coverage across global/layer/quant selected keys.
- 2026-03-07: Documented in-test policy boundaries for implicit-default vs explicit-selection no-job manifest emission, added side-effect assertions for axis-normalization and missing-matplotlib early failures, added corrupted `tables_write_manifest.json` fallback coverage, and relaxed load-error cause checks from backend-specific `parquet` text to non-empty structured error fields.
- 2026-03-07: Added coverage for `plots: null` as an explicit default/unset policy case, plus valid-JSON/wrong-schema `tables_write_manifest.json` fallback behavior (non-dict `artifacts` and selected entry missing `path`).
- 2026-03-08: Removed non-module loader special-casing so explicit selected-missing artifacts always record errors, strengthened manifest PNG helper to assert non-empty files, and expanded default legacy-scan partial-input coverage to assert `plots_write_manifest.json` requested/artifact semantics.
- 2026-03-08: Tightened remaining contract sharp edges by making missing-run-dir test path deterministic/nonexistent, replacing broad missing/not-found regexes with exact selected-artifact error text assertions, and adding explicit no-extra-PNG checks in success-path tests.
- 2026-03-09: Added fail-first coverage and implementation fix for non-object `analysis_config.json` roots (list/null now fail with explicit validation errors), removed duplicated helper assertion noise, and added a repo-level pre-PR Contract Matrix Review section to `AGENTS.md` for code/test review structure.
- 2026-03-20: Added a new fail-first plotting review pass for direct-data behavior under explicit table selection, including enabled `expert_layer_heatmaps` surviving `plots.artifact_keys` / `plots.artifact_keys: []`, and missing enabled direct inputs becoming audited errors instead of silent no-job returns.
- 2026-03-20: Tightened the malformed direct `quant_sim` contract to pin the exact direct-data artifact key and audit fields, narrowed the older empty-selection test to the direct-data-disabled policy case, and reran the suite to confirm the remaining 5 failures still isolate the `build_plots.py` implementation gap rather than test ambiguity.
- 2026-03-20: Landed the direct-data selection fix in `build_plots.py` by processing explicitly enabled direct-data plots outside the table-selection branch, preserving default implicit heatmap behavior for absent inputs, and auditing missing explicitly enabled direct inputs. Verified green with `tests.test_build_plots_contract`, `tests.test_plot_inputs_contract`, and `tests.test_build_tables_contract`.
- 2026-03-20: Added a second fail-first hardening pass for the plotting subset after adversarial review, then tightened it again to require malformed direct config to fail before discovery rather than just before writes. The current fail-first gaps are now narrower: the quant direct-config branch still validates too late, both direct `enabled: 0` bool holes remain, and direct quant no-job paths still load `matplotlib` too early.
- 2026-03-20: Landed the second plotting hardening pass in `build_plots.py`: direct-plot config now validates before discovery/side effects, `enabled` checks no longer accept `0`/falsey non-bools, and direct quant no-job paths skip before loading `matplotlib`. Verified green with `tests.test_build_plots_contract`, `tests.test_plot_inputs_contract`, and `tests.test_build_tables_contract`.
- 2026-03-20: Added a third fail-first plotting hardening pass for explicitly enabled expert heatmap no-job behavior. The remaining gap is now specific and narrow: explicit heatmap no-job inputs still collapse into silent success instead of recording a `skipped` manifest entry in both implicit-selection and explicit-empty-selection paths.
- 2026-03-20: Landed the explicit heatmap no-job audit fix in `build_plots.py`: explicitly enabled heatmap requests now emit a `skipped` direct-data artifact entry when the input exists but has no plottable rows, while default implicit heatmap no-job behavior remains unchanged. Verified green with `tests.test_build_plots_contract`, `tests.test_plot_inputs_contract`, and `tests.test_build_tables_contract`.
